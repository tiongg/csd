spring:
  datasource:
    url: jdbc:postgresql://db:5432/postgres
    username: postgres
    password: password

  flyway:
    enabled: true
    locations: classpath:db/migration

  jooq:
    sql-dialect: POSTGRES
